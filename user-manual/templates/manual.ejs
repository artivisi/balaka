<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#eff6ff',
                            100: '#dbeafe',
                            200: '#bfdbfe',
                            300: '#93c5fd',
                            400: '#60a5fa',
                            500: '#3b82f6',
                            600: '#2563eb',
                            700: '#1d4ed8',
                            800: '#1e40af',
                            900: '#1e3a8a',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        .prose h2 { font-size: 1.5rem; font-weight: 700; margin-top: 2rem; margin-bottom: 1rem; color: #1e3a8a; }
        .prose h3 { font-size: 1.25rem; font-weight: 600; margin-top: 1.5rem; margin-bottom: 0.75rem; color: #374151; }
        .prose h4 { font-size: 1.1rem; font-weight: 600; margin-top: 1rem; margin-bottom: 0.5rem; color: #4b5563; }
        .prose p { margin-bottom: 1rem; line-height: 1.75; }
        .prose ul, .prose ol { margin-bottom: 1rem; padding-left: 1.5rem; }
        .prose li { margin-bottom: 0.5rem; }
        .prose table { width: 100%; border-collapse: collapse; margin: 1rem 0; }
        .prose th, .prose td { border: 1px solid #e5e7eb; padding: 0.75rem; text-align: left; }
        .prose th { background-color: #f9fafb; font-weight: 600; }
        .prose code { background-color: #f3f4f6; padding: 0.125rem 0.375rem; border-radius: 0.25rem; font-size: 0.875rem; }
        .prose strong { font-weight: 600; }
        .screenshot-container { margin: 1.5rem 0; }
        .screenshot-container img { border: 1px solid #e5e7eb; border-radius: 0.5rem; box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1); }
        @media print {
            .no-print { display: none !important; }
            .prose { font-size: 12pt; }
            .screenshot-container img { max-width: 100%; page-break-inside: avoid; }
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Header -->
    <header class="bg-primary-700 text-white shadow-lg sticky top-0 z-50 no-print">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                <div class="flex items-center">
                    <svg class="w-8 h-8 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                    </svg>
                    <h1 class="text-xl font-bold"><%= title %></h1>
                </div>
                <div class="flex items-center space-x-4 text-sm">
                    <span class="hidden sm:inline">Versi <%= version %></span>
                    <button onclick="window.print()" class="px-3 py-1 bg-primary-600 rounded hover:bg-primary-500 transition">
                        Cetak
                    </button>
                </div>
            </div>
        </div>
    </header>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="lg:grid lg:grid-cols-12 lg:gap-8">
            <!-- Sidebar Navigation -->
            <aside class="hidden lg:block lg:col-span-3 no-print">
                <nav class="sticky top-24 bg-white rounded-lg shadow-sm border border-gray-200 p-4">
                    <h2 class="text-sm font-semibold text-gray-500 uppercase tracking-wider mb-4">Daftar Isi</h2>
                    <ul class="space-y-2">
                        <% sections.forEach(function(section, index) { %>
                        <li>
                            <a href="#<%= section.id %>"
                               class="flex items-center text-sm text-gray-700 hover:text-primary-600 hover:bg-primary-50 px-3 py-2 rounded-lg transition-colors">
                                <span class="w-6 h-6 flex items-center justify-center bg-primary-100 text-primary-700 rounded-full text-xs font-medium mr-2">
                                    <%= index + 1 %>
                                </span>
                                <%= section.title %>
                            </a>
                        </li>
                        <% }); %>
                    </ul>

                    <div class="mt-6 pt-4 border-t border-gray-200">
                        <p class="text-xs text-gray-500">
                            Terakhir diperbarui:<br>
                            <span class="font-medium"><%= lastUpdated %></span>
                        </p>
                    </div>
                </nav>
            </aside>

            <!-- Mobile Navigation -->
            <div class="lg:hidden mb-6 no-print">
                <details class="bg-white rounded-lg shadow-sm border border-gray-200">
                    <summary class="px-4 py-3 cursor-pointer font-medium text-gray-700">
                        Daftar Isi
                    </summary>
                    <ul class="px-4 pb-4 space-y-2">
                        <% sections.forEach(function(section, index) { %>
                        <li>
                            <a href="#<%= section.id %>" class="text-sm text-primary-600 hover:underline">
                                <%= index + 1 %>. <%= section.title %>
                            </a>
                        </li>
                        <% }); %>
                    </ul>
                </details>
            </div>

            <!-- Main Content -->
            <main class="lg:col-span-9">
                <% sections.forEach(function(section, index) { %>
                <section id="<%= section.id %>" class="bg-white rounded-lg shadow-sm border border-gray-200 mb-8 overflow-hidden">
                    <!-- Section Header -->
                    <div class="bg-gradient-to-r from-primary-600 to-primary-700 px-6 py-4">
                        <div class="flex items-center">
                            <span class="w-10 h-10 flex items-center justify-center bg-white/20 text-white rounded-full text-lg font-bold mr-4">
                                <%= index + 1 %>
                            </span>
                            <h2 class="text-xl font-bold text-white"><%= section.title %></h2>
                        </div>
                    </div>

                    <!-- Section Content -->
                    <div class="p-6">
                        <div class="prose max-w-none">
                            <%- section.content.replace(/^## /gm, '<h2>').replace(/^### /gm, '<h3>').replace(/^#### /gm, '<h4>')
                                .replace(/\n## /g, '</p>\n<h2>').replace(/\n### /g, '</p>\n<h3>').replace(/\n#### /g, '</p>\n<h4>')
                                .replace(/\*\*([^*]+)\*\*/g, '<strong>$1</strong>')
                                .replace(/`([^`]+)`/g, '<code>$1</code>')
                                .replace(/^\| (.+) \|$/gm, function(match, content) {
                                    return '<tr>' + content.split(' | ').map(function(cell) { return '<td>' + cell.trim() + '</td>'; }).join('') + '</tr>';
                                })
                                .replace(/^\|[-\s|]+\|$/gm, '')
                                .replace(/(<tr>.*<\/tr>\n)+/g, function(match) { return '<table>' + match + '</table>'; })
                                .replace(/^(\d+)\. (.+)$/gm, '<li>$2</li>')
                                .replace(/^- (.+)$/gm, '<li>$1</li>')
                                .replace(/(<li>.*<\/li>\n)+/g, function(match) { return '<ul>' + match + '</ul>'; })
                                .replace(/\n\n/g, '</p>\n<p>')
                                .replace(/<h(\d)>([^<]+)(<\/p>)?/g, '<h$1>$2</h$1>')
                            %>
                        </div>

                        <% if (section.pageData && section.pageData.length > 0) { %>
                        <!-- Screenshots -->
                        <div class="mt-8 border-t border-gray-200 pt-6">
                            <h3 class="text-lg font-semibold text-gray-800 mb-4">Tampilan Layar</h3>
                            <div class="space-y-6">
                                <% section.pageData.forEach(function(page) { %>
                                <% if (page.captured) { %>
                                <div class="screenshot-container">
                                    <div class="flex items-center mb-2">
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800 mr-2">
                                            <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                                            </svg>
                                            <%= page.name %>
                                        </span>
                                    </div>
                                    <a href="screenshots/<%= page.screenshot %>" target="_blank" class="block">
                                        <img src="screenshots/<%= page.screenshot %>"
                                             alt="<%= page.name %>"
                                             class="w-full rounded-lg hover:shadow-lg transition-shadow cursor-zoom-in">
                                    </a>
                                    <p class="mt-2 text-sm text-gray-600"><%= page.description %></p>
                                </div>
                                <% } else { %>
                                <div class="screenshot-container">
                                    <div class="flex items-center mb-2">
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-600 mr-2">
                                            <%= page.name || page.id %>
                                        </span>
                                    </div>
                                    <div class="bg-gray-100 rounded-lg p-8 text-center">
                                        <svg class="w-12 h-12 mx-auto text-gray-400 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                                        </svg>
                                        <p class="text-sm text-gray-500">Screenshot belum tersedia</p>
                                    </div>
                                </div>
                                <% } %>
                                <% }); %>
                            </div>
                        </div>
                        <% } %>
                    </div>
                </section>
                <% }); %>

                <!-- Footer -->
                <footer class="text-center text-sm text-gray-500 py-8">
                    <p>&copy; <%= new Date().getFullYear() %> Aplikasi Akunting - ArtiVisi</p>
                    <p class="mt-1">Dokumentasi dibuat secara otomatis menggunakan Playwright</p>
                </footer>
            </main>
        </div>
    </div>

    <!-- Smooth scroll for anchor links -->
    <script>
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });
    </script>
</body>
</html>
